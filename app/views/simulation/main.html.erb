 <%# <script type="module" src="main.js "></script> %>



 
 <script src="//daybrush.com/moveable/release/latest/dist/moveable.min.js"></script>
 <script src="https://html2canvas.hertzen.com/dist/html2canvas.js"></script>
 <%= javascript_pack_tag 'simulation/main' %>

<div class="container-fluild">
  <div class="page_title" >
    <h1>Simulation</h1>
  </div>
  <div class="row">
    <%# 耳画像 %>
    <div class="col-6 align-self-end">
      <div class="ear-image" width="300" height="360" style="margin-left: 40%; position: relative;">
        <canvas id="ear-image" width="300" height="360"></canvas>
        <div class="add-drag-and-drop" >
        </div>
      </div>
    </div>

    <%# ピアス画像データ%>
    <div class="pierce-images col-4">
      <div class="row gx-5">
        <% if @pierce_image.present? %>
          <% @pierce_image.each_with_index do |pierce_image, n| %>
            <div class="card col-3 text-center">
              <figure id= "<%= "image-#{n + 1}"%>" onClick="copy_image(this.id)" style="padding: 0; margin: 0;">
                <%= image_tag pierce_image.pierce_image.url, class:"card-img-top", alt:"pierce_image" %>
              </figure>
              <div class="card-body" style="margin: 0;">
                <p class="card-title"><%= pierce_image.pierce_image_name%></p>
              </div>
            </div>
          <% end %>
        <% else %>
          <p>”データがありません"</p>
      <% end %>
      </div>
    </div>
    <div class="button-group row">
      <%# Downloadボタン%>
      <div class="download-button col-4 text-right">
        <input id="download-btn" type="button" value="Download" class="btn btn-secondary">
      </div>
      <%# Cleanボタン%>
      <div class="clean-button col-4 align-self-end">
        <button id="clean-btn" class="btn btn-dark" onClick="removeMoveable()">clean</button>
      </div>
    </div>
  </div>
</div>
  
