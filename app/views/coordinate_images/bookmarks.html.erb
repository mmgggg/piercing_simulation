<% content_for(:title, "My Bookmarks") %>
<div class="container">
  <h1 class="text-center">My Bookmarks</h1>
  <div class="row g-5">
   <% if @bookmark_coordinate_images.present? %>
     <%= render @bookmark_coordinate_images %>
   <% else %>
    <p>お気に入りはありません</p>
   <% end %>
  </div>
  <%= paginate @bookmark_coordinate_images %>
</div>