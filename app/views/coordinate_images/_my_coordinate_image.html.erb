<div class="col-sm-12 col-lg-4 mb-3">
  <div id="coordinate_image-id-<%= my_coordinate_image.id %>">
    <div class="card">
        <%= image_tag my_coordinate_image.image_url, class: 'card-img-top', size: '300x200' %>
      <div class="card-body">
        <%# <div class='mr10 float-right'>
            <%# <%= link_to edit_board_path(board.id), id: "button-edit-#{board.id}" do %>
              <%# <%= icon 'fa', 'pen' %>
            <%# <% end %>
            <%# <%= link_to board_path(board.id), id: "button-delete-#{board.id}", method: :delete, data: {confirm: '削除しますか？'} do %>
              <%# <%= icon 'fas', 'trash' %>
            <%# <% end %>
          <%# <% else %>
            <%# <%= render 'bookmark_area', board: board%>
          <%# <% end %>
        <%# </div> %>
      </div>
    </div>
  </div>
</div>